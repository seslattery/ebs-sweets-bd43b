backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: "/static/images/netlify-uploads" # Media files will be stored in the repo under images/uploads
public_folder: "/images/netlify-uploads"
